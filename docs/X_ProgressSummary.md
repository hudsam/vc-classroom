# Ringkasan Tahap Analisis: Platform Sewa Smart Classroom
**Tipe Dokumen:** Cross-Document Summary (bukan pengganti 4 dokumen sumber)
**Tanggal:** 8 Agustus 2026
**Disusun Setelah:** Analisis & koreksi terhadap `01_ProductDiscovery.md`, `02_ProductRequirementDocument.md`, `03_FunctionalRequirementDocument.md`, `04_InformationArchitecture.md`
**Tujuan:** Memberi satu titik baca cepat sebelum tim masuk ke tahap **Desain UI/UX**, sekaligus daftar tindakan prioritas agar proses jangka pendek menuju MVP tidak terhambat oleh gap yang masih tersisa antar dokumen.

> **Update tindak lanjut (siklus 1 — P0-1 & P0-2):** PRD naik ke v1.2 (NFR kini ber-ID, Success Metrics & Milestone disinkronkan ke PD, Bagian 10 dipulihkan, FR-09 ditambahkan) dan FRD naik ke v1.2 (traceability Modul 7 dialihkan dari NFR-S03/gap ke FR-09).
>
> **Update tindak lanjut (siklus 2 — P0-3, P1-4, P1-5, P1-6):** PD naik ke v1.2 (§8.1 tabel kerja penentuan harga, §8.2 draft kontekstual kebijakan refund subscription); FRD naik ke **v1.3** (Late Checkout Penalty diputuskan keluar dari MVP dan ditangani manual oleh petugas Ops; catatan penundaan endpoint quota/vault ke sesi backend); IA naik ke v1.2 (screen 1.6 Kebijakan & Ketentuan ditambahkan; tabel Cakupan Screen Hierarchy ditambahkan agar Temuan #6 tidak berulang).
>
> **Update tindak lanjut (siklus 3 — P1-4 tuntas, P1-5 tuntas untuk cakupan minimal):** PD naik ke v1.3 (harga final dari §8.1 dipindahkan ke ringkasan §8 utama, menutup inkonsistensi "indikatif" yang tertinggal). IA naik ke v1.3 (H-05 s/d H-13 ditambahkan — Subscription, Account Profile, dan 7 screen Admin Portal — menuntaskan cakupan minimal P1-5; tabel §4.0 kini 13/28 Done).
>
> **Update tindak lanjut (siklus 4 — cross-check final sebelum UI/UX & prototyping):** Seluruh ID (FR-01-09, NFR-P/S/R-xx, FEAT-xx, H-01-13, kode error, screen 1.1-5.7) diverifikasi 1:1 valid lintas 4 dokumen — tidak ditemukan ID yatim. Tanggal roadmap PD§9↔PRD§9 dan harga PD§8↔§8.1 dikonfirmasi tetap sinkron. **2 temuan baru ditemukan & langsung diperbaiki di IA (naik ke v1.4):** (1) screen 3.5 punya 3 nama berbeda ("Subscriptions"/"Subscription Wallet"/"Subscription") — distandardisasi; (2) §7 Catatan Permission belum punya baris untuk 3.5 & 3.6 meski sudah Done — ditambahkan. **1 gap dicatat, tidak diperbaiki sepihak:** FRD §4 RBAC belum punya baris permission eksplisit untuk Audit Logs (5.5) — H-11 memakai asumsi kerja yang ditandai jujur, dilacak sebagai P2-10 baru di §6.

> **Update tindak lanjut (siklus 5 — awal Tahap 3 Prototyping, 9 Agustus 2026):** Proyek memasuki **Tahap 3 — Prototyping (Google AI Studio)** sesuai `docs/GEMINI_SYSTEM_PROMPT.md`. **5 dari 13 screen** yang sudah punya content hierarchy (IA §4) kini punya prototipe HTML statis (Tailwind CDN): H-01 (`index.html`), H-02 (`in-room.html`), H-06 (`profile.html`), H-07 (`admin/dashboard.html`), H-08 (`admin/rooms.html`) — lihat tabel tracking baru di §8. Cross-check prototipe vs 4 dokumen sumber **tidak menemukan penyimpangan istilah/angka** (kode error, harga, RBAC show/hide semua konsisten 1:1). **3 temuan baru dicatat sebagai P2-11 s/d P2-13 di §6** (bukan diperbaiki sepihak di kode): tautan Quick Link admin yang belum disambungkan ke halaman yang sudah ada, dua stack front-end yang tumpang tindih (scaffold React/Vite kosong vs prototipe HTML aktual), dan status Tahap 3 di `GEMINI_SYSTEM_PROMPT.md` yang belum disinkronkan dengan progres nyata. Seluruh file prototipe pada siklus ini masih **belum di-commit ke git** (untracked) — dicatat sebagai catatan operasional, bukan gap dokumen.

> **Update tindak lanjut (siklus 6 — analisa & perbaikan Skema Database, Tahap 4 — Google Antigravity, 9 Agustus 2026):** `README/3-DBSchema.md` (Draf Desain Skema Produksi) dianalisis lintas 4 dokumen sumber + source code prototipe, naik dari **v1.0 ke v1.1**. Ditemukan & **langsung diperbaiki**: 1 tautan dokumen rusak, 2 inkonsistensi istilah role vs `role_enum`, dan **1 bug logika** (penanganan kuota "Unlimited" Enterprise yang akan salah memicu `QUOTA_EXCEEDED`). Ditemukan & **ditambahkan langsung ke skema**: 3 tabel yang belum ada meski fiturnya sudah terdefinisi di FRD/IA (`booking_cancellations`, `wallet_transactions`, `door_access_attempts`) beserta alur workflow & indeks pendukungnya. Detail lengkap di **§9** (baru). **3 keputusan bisnis baru dicatat sebagai P2-14 s/d P2-16 di §6** (bukan diputuskan sepihak di skema) — kuota storage Basic/Enterprise, nasib saldo wallet saat akun dihapus, dan SLA/mekanisme pencairan refund subscription.

> **Update tindak lanjut (siklus 7 — cross-check `README/3-DBQuery.md` vs `README/3-DBSchema.md`, Tahap 4 lanjutan, 9 Agustus 2026):** `README/3-DBQuery.md` (kueri DDL inisialisasi database PostgreSQL 17 di Aiven) dianalisis baris-per-baris terhadap `README/3-DBSchema.md` v1.1 — skema acuannya sendiri. 19 `CREATE TABLE`, 11 indeks, seluruh FK/`ON DELETE`, dan seluruh nilai `CHECK` dicocokkan satu-satu. **Sebagian besar konsisten 1:1** (tipe data, urutan dependensi FK, aksi `ON DELETE`, dan seluruh 11 indeks §6 DBSchema termasuk di DBQuery §4 tanpa ada yang hilang). **3 temuan dicatat** (bukan diperbaiki sepihak, sesuai arahan — hanya dilaporkan & didaftarkan sebagai backlog baru **P1-7, P2-17, P2-18** di §6): (1) tipe data kolom `wallet_transactions.reference_id` berbeda antar dokumen — DBSchema/ERD menyatakan `VARCHAR(100)` tanpa FK eksplisit, DBQuery mengimplementasikan `UUID` dengan FK eksplisit ke `booking_cancellations.id` (versi DBQuery yang secara teknis benar karena PK targetnya UUID); (2) constraint `recordings.stopped_at > started_at` yang terdokumentasi di DBSchema §3.10 tidak ada di DDL DBQuery, padahal pola serupa pada `room_maintenance_logs.ended_at` sudah diimplementasikan; (3) 6 aturan bisnis di kolom "Batasan & Aturan Bisnis" DBSchema (format email, panjang min. nama, jendela jam booking 07:00-22:00, durasi booking kelipatan 30 menit, kelipatan kuota 0.5 jam, batas 500 karakter kolom alasan) tidak punya `CHECK` constraint padanan di DBQuery — kemungkinan besar disengaja (validasi level aplikasi), tapi ambigu karena dokumen tidak membedakan mana yang wajib di-DB vs app-layer. Detail lengkap & tabel temuan di **§10** (baru).

> **Update tindak lanjut (siklus 8 — cross-check `README/4-APIDesign.md` vs FRD/PRD/IA/DBSchema/DBQuery, Tahap 4 lanjutan, 9 Agustus 2026):** `README/4-APIDesign.md` v1.0 (desain REST API + MQTT, penyelesaian item tertunda P1-6) dianalisis terhadap seluruh dokumentasi terkait penggunaan & transaksi data (FRD §4/§5/§7/§8, IA §4.1, DBSchema §3/§4, DBQuery). **6 temuan dicatat** (dilaporkan saja, tidak diperbaiki sepihak di `4-APIDesign.md`, konsisten dengan arahan siklus sebelumnya) — didaftarkan sebagai backlog baru **P1-8, P1-9, P2-19 s/d P2-21** di §6, plus 1 catatan tambahan pada **P2-10** yang sudah ada: (1) payload MQTT `/classroom/door/unlock` (§6.2) memakai nilai `reason: "ROOM_MISMATCH"` yang tidak ada di `door_access_result_enum` DBSchema/DBQuery (hanya `SUCCESS`/`TOKEN_EXPIRED_OR_INVALID`) maupun matriks error FRD §8 — jika diimplementasikan apa adanya, `INSERT` ke `door_access_attempts.result` akan gagal melanggar `CHECK` constraint; (2) enum `add_ons` menambahkan `live_streaming` yang tidak punya definisi FR/FEAT/pricing/kolom DB apa pun (hanya disebut sepintas sebagai item roadmap bisnis di PD §8, bukan fitur MVP siap-implementasi); (3) `GET /rooms` tidak menyediakan query parameter untuk 2 dari 4 filter yang didokumentasikan IA §4.1 (kapasitas & rentang harga), sementara skema responsnya menyisipkan objek `hourly_rates` 3-tingkat per ruangan yang tidak cocok dengan model 1 ruangan = 1 `capacity_tier` + 1 `price_per_hour` di DBSchema, dan tidak menampilkan `capacity_tier` itu sendiri; (4) field `recording.status`/`transcription.status` pada `GET /bookings/{id}/vault` memakai istilah `COMPLETED` padahal DBSchema menyimpan `READY` untuk kedua tabel (`recordings.status`, `ai_transcriptions.status`) — drift istilah yang berisiko salah peta saat implementasi; (5) daftar opsi `payment_method` pada komentar `POST /payments/charge` (§3.2) tidak lengkap — hanya 4 dari 7 nilai `payment_method_enum` DBSchema yang disebut (`VA_BNI` & `EWALLET_SHOPEEPAY` hilang); (6) §10 `4-APIDesign.md` mengklaim status **P2-10 "Selesai"** (RBAC Audit Logs dibatasi `SUPER_ADMIN`), padahal FRD §4 RBAC dikonfirmasi ulang **masih belum punya baris permission untuk Audit Logs** — klaim ini bertentangan dengan keputusan eksplisit siklus 4 bahwa P2-10 butuh sign-off produk/keamanan, bukan keputusan sepihak; status kanonis P2-10 di §6 **tetap ⏳ Belum dimulai**. Detail lengkap & tabel temuan di **§11** (baru) — disusun agar tidak menduplikasi isi `4-APIDesign.md` sendiri, hanya merujuk balik ke bagiannya.

> **Update tindak lanjut (siklus 9 — awal Tahap 5, Implementasi API Backend, 9 Agustus 2026):** Database (`README/3-DBQuery.md`) sudah **dibuat/live di platform cloud (Aiven PostgreSQL)**. Proyek memasuki **Tahap 5 — Implementasi API Backend**, tujuannya menghubungkan 5 prototipe UI statis yang sudah ada (`index.html`, `in-room.html`, `profile.html`, `admin/dashboard.html`, `admin/rooms.html`) ke database nyata via endpoint REST yang sudah dirancang di `README/4-APIDesign.md` v1.0. **Backlog bertahap ditambahkan di §12 (baru)** — 26 task singkat (API-01 s/d API-26) dikelompokkan dalam 10 tahap kerja (setup fondasi → auth → katalog/booking → payment → IoT/in-room → vault → subscription → admin portal → integrasi frontend → testing). 3 backlog lama yang berisiko jadi blocker implementasi nyata (**P1-8** `ROOM_MISMATCH`, **P1-9** `live_streaming`, **P2-19** filter/skema `GET /rooms`) ditandai eksplisit di task terkait agar diputuskan **sebelum** endpoint tersebut dikerjakan, bukan sesudah.

> Dokumen ini **tidak menggantikan** keempat dokumen sumber — setiap klaim di sini merujuk balik ke bagian aslinya. Jika ada perbedaan, dokumen sumber yang menjadi acuan sah.

---

## 1. Peta Dokumen

| # | Dokumen | Versi | Menjawab | Target Audiens Utama |
| :--- | :--- | :--- | :--- | :--- |
| 01 | `01_ProductDiscovery.md` (PD) | **1.3** | **WHY** — Visi, persona, JTBD, kompetitor, SWOT, model bisnis (harga final + draft kebijakan refund subscription), roadmap kuartalan | Bisnis, PM, UI/UX, Engineering |
| 02 | `02_ProductRequirementDocument.md` (PRD) | 1.2 | **WHAT** — Scope (kini termasuk Manajemen Akun), 9 Functional Requirements (+FR-09), Non-Functional Requirements ber-ID, Success Metrics, User Stories & AC | PM, Engineering, QA |
| 03 | `03_FunctionalRequirementDocument.md` (FRD) | 1.3 | **HOW (system & logic)** — Modul fitur teknis (Modul 7 kini resmi ditelusuri ke FR-09), workflow, RBAC, kontrak API, validasi data, business rules (Late Checkout kini manual-MVP), error handling | Engineering, IoT Engineer, QA |
| 04 | `04_InformationArchitecture.md` (IA) | **1.4** | **HOW (navigasi & struktur)** — Sitemap (+1.6 Kebijakan & Ketentuan), navigation flow, 13/28 screen hierarchy Done (+tabel cakupan §4.0), user/task flow, implikasi permission ke UI (kini lengkap untuk seluruh screen Done) | UI/UX Designer, Frontend Engineer |

Rantai baca yang benar: **PD → PRD → FRD → IA → (tahap berikutnya) Desain UI/UX**. Setiap dokumen sudah menegaskan prinsip anti-redundansi masing-masing (lihat §1 di tiap dokumen) — dokumen ini merangkumnya tanpa mengulang isi penuh.

---

## 2. Ringkasan per Dokumen

### 2.1 Product Discovery (PD)
* **Visi:** Ekosistem smart classroom yang mendemokratisasi akses teknologi pengajaran (booking mandiri, rekaman otomatis, live streaming).
* **3 Persona:** Dr. Aris (dosen independen), Siti Rahma (corporate trainer), Bima Utama (content creator) — masing-masing dipetakan ke JTBD terkait (§5.1 PD).
* **3 Problem (P1-P3):** aksesibilitas studio terbatas, kompleksitas otomasi rekaman, kebutuhan kelas insidental/blended — dipetakan 1:1 ke solusi produk.
* **Model bisnis:** Pay-per-use — **harga final** Rp150rb (Small) / Rp250rb (Medium) / Rp350rb (Large) per jam, disetujui 8 Ags 2026; Subscription 3 tier — **harga final** Basic Rp1,2jt / Pro Rp4,9jt / Enterprise Rp11,9jt per bulan; add-on (transkripsi AI, storage, live streaming multi-platform).
* **§8.1 Tabel Kerja Penentuan Harga:** worksheet penentuan harga — sudah **selesai diisi & disetujui** tim bisnis, dipertahankan sebagai jejak audit dasar perhitungan (acuan kompetitor, estimasi biaya operasional, margin).
* **§8.2 Kebijakan Refund Subscription (baru, draft kontekstual):** kerangka awal — 100% refund dalam 30 hari pertama aktivasi, tidak ada refund setelahnya, plus alur komunikasi & eskalasi. Ditandai tegas belum final, menunggu validasi bisnis/legal sebelum masuk halaman Kebijakan & Ketentuan (IA 1.6).
* **Roadmap:** Q3 2026 Foundation & MVP (booking core, payment dasar, IoT gateway, rekaman dasar, Beta Launch 1 lokasi) → Q4 2026 Enhancement & AI (in-room controller, AI transcription, subscription wallet) → Q1 2027 Scale (multi-lokasi, integrasi LMS, B2B portal).

### 2.2 Product Requirement Document (PRD)
* **Scope MVP:** Manajemen akun & autentikasi, web app responsive, booking & payment self-service, integrasi smart lock, in-room controller, auto-recording + transkripsi dasar. **Out of scope Fase 1:** aplikasi mobile native, pengadaan hardware, integrasi LMS enterprise, marketplace tutor, *pembatalan manual via admin*.
* **9 Functional Requirements (FR-01 s/d FR-09):** Eksplorasi Ruang, Engine Reservasi, Gateway Pembayaran, Integrasi Smart Lock, In-Room Controller, Penyimpanan Cloud, AI Transkripsi, Manajemen Kuota, **Manajemen Akun & Autentikasi Pengguna (FR-09 — baru di v1.2)**.
* **Non-Functional Requirements (kini ber-ID):** performa `NFR-P01-P04` (response time, concurrency, IoT latency, video processing time), keamanan `NFR-S01-S03` (token time-bound, TLS 1.3/AES-256, OAuth2/JWT), keandalan `NFR-R01-R02` (uptime 99.5%, local caching fail-safe).
* **Success Metrics:** Success Access Rate (>99%, ref. NFR-P03), Booking Completion Rate (>75%), Video Auto-Sync Time (<15 menit, ref. NFR-P04/NFR-R02), Classroom Utilization Rate (>30%/bulan).
* **3 User Story + AC:** Pemesanan & Pembayaran, Masuk via Smart Lock, Kontrol Perekaman.
* **Risiko utama:** internet down saat sesi, smart lock gagal karena listrik mati, pembatalan mendadak — masing-masing sudah punya mitigasi.
* **§10 Keterkaitan Antar Dokumen:** tabel *source of truth* + rekomendasi anti-redundansi jangka pendek/menengah/panjang.

### 2.3 Functional Requirement Document (FRD)
* **7 Modul, 15 Fitur Teknis (FEAT-xx):** Catalog, Booking Engine (+ Cancellation & Refund Engine), Payment Gateway, IoT Smart Lock, In-Room Controller, Cloud Video Vault, **User & Access Management (kini resmi ditelusuri ke FR-09)**.
* **4 Workflow end-to-end:** Booking & Pembayaran, Akses IoT & Kontrol Rekaman, Cloud Upload & AI Transcription, Pembatalan & Refund.
* **RBAC 5 role:** Guest, Member, Premium Member, Studio Admin, Super Admin — 8 baris permission termasuk *Cancel Own Booking*.
* **9 kontrak/ringkasan API** (REST + kanal MQTT untuk IoT), termasuk endpoint Cancel Booking yang baru.
* **Validasi data & 5 Business Rule** (access window buffer, cancellation policy, **late checkout penalty — kini ditangani manual oleh petugas Ops untuk MVP, bukan otomatis via sensor**, max concurrent lock, cancellation guard).
* **9 skenario Error Handling** dengan HTTP status, pesan user-facing, dan fail-safe.

### 2.4 Information Architecture (IA)
* **Sitemap 28 screen** dalam 5 area (Public, Booking Engine, User Dashboard, In-Room Controller, Admin Portal) — termasuk **1.6 Kebijakan & Ketentuan (baru, placeholder)** untuk menampung kebijakan refund subscription dari PD §8.2.
* **Navigation Flow** memisahkan eksplisit kanal fisik (QR di Smart Lock) vs kanal digital (WiFi → In-Room Dashboard), plus jalur pembatalan & fallback kuota habis.
* **13 dari 28 screen (46%)** sudah punya *content hierarchy* detail: Catalog (H-01), In-Room Controller (H-02), Checkout (H-03), Cloud Video Vault (H-04), Subscription (H-05), Account Profile (H-06), dan seluruh 7 screen Admin Portal (H-07 s/d H-13) — menuntaskan cakupan minimal P1-5. **§4.0 Tabel Cakupan Screen Hierarchy** melacak status Done/Pending seluruh 28 screen agar gap serupa Temuan #6 tidak lagi lolos tanpa disadari.
* **2 User Flow** (Discovery→Checkout, Cancellation & Refund) dan **3 Task Flow** mikro (Buka Pintu, Kontrol Rekaman, Pembatalan Mandiri).
* **§7 Permission & Access Notes:** menerjemahkan RBAC FRD menjadi keputusan show/hide/disable per screen untuk desainer.

---

## 3. Traceability End-to-End (Contoh Rantai Utuh)

Tabel ini membuktikan keempat dokumen benar-benar tersambung dari alasan bisnis sampai ke layar — berguna sebagai peta cepat saat desainer/engineer butuh konteks "kenapa fitur ini ada".

| Persona (PD) | JTBD (PD) | FR (PRD) | Fitur Teknis (FRD) | Screen (IA) |
| :--- | :--- | :--- | :--- | :--- |
| Dr. Aris, Siti Rahma, Bima | JTBD 1: Pemesanan & Akses | FR-01, FR-02, FR-03, FR-04 | FEAT-CAT-01/02, FEAT-BKG-01/02, FEAT-PAY-01, FEAT-IOT-01/02 | 1.2, 2.1-2.4, H-03, 3.3 |
| Siti Rahma, Bima | JTBD 2: Perekaman & Kontrol | FR-05 | FEAT-CTL-01/02 | 4.1-4.4 (H-02) |
| Dr. Aris, Bima | JTBD 3: Pasca Perekaman | FR-06, FR-07 | FEAT-VLT-01/02 | 3.4 (H-04) |
| *(seluruh persona, lintas JTBD)* | — | FR-08 | FEAT-PAY-02 | 3.5, opsi bayar di H-03 |
| *(seluruh persona)* | — | FR-09 | FEAT-USR-01/02 | 1.4, 3.6, 5.7 |
| *(seluruh persona)* | — | *(diturunkan dari Business Rule PRD §8.1/FRD §7.2, bukan FR eksplisit)* | FEAT-BKG-03 | 3.2, 3.7 |

---

## 4. Temuan Kritis

Beberapa gap berikut **sudah teridentifikasi** selama proses analisis dokumen 01-04. Status terkini ditandai per baris — lihat §6 untuk log tindak lanjut yang lebih rinci per langkah perbaikan.

| # | Temuan | Dampak jika Dibiarkan | Dokumen Terdampak | Status |
| :--- | :--- | :--- | :--- | :---: |
| 1 | ~~PRD §5 (NFR) belum memiliki ID individual per butir~~ — **sudah diperbaiki**: NFR-P01-P04, NFR-S01-S03, NFR-R01-R02 ditambahkan di PRD v1.2. | ~~Engineer tidak menemukan definisi persis ID yang dirujuk FRD.~~ | PRD §5, FRD §2 & §6 | ✅ Selesai |
| 2 | ~~PRD §6 & §9 belum sinkron tanggal dengan roadmap PD §9~~ — **sudah diperbaiki**: tanggal IoT Smart Lock & Payment Gateway Integration disamakan, task AI Transcription dipisah, PRD §10 dipulihkan. | ~~Tim mengacu tanggal berbeda saat sprint planning.~~ | PRD §9, PD §9 | ✅ Selesai |
| 3 | ~~Tidak ada FR resmi untuk User & Access Management~~ — **sudah diperbaiki**: FR-09 ditambahkan di PRD §4, traceability FRD §2.7 dialihkan dari NFR-S03/gap ke FR-09; PRD §3.1 & Scope Matrix juga diperbarui. | ~~Tim backend tidak punya acceptance criteria resmi untuk fondasi akun pengguna.~~ | PRD §3, §4; FRD §2, §2.7 | ✅ Selesai |
| 4 | ~~Business Rule "Late Checkout Penalty" (FRD §7.3) mengasumsikan mekanisme deteksi kehadiran (occupancy sensor) yang tidak ada di modul fitur manapun.~~ — **sudah diputuskan**: dikeluarkan dari cakupan MVP, deteksi & penindakan dilakukan manual oleh petugas Ops untuk sementara; otomasi sensor jadi opsi pasca-MVP. | ~~Rule tidak bisa diimplementasikan sampai ada keputusan.~~ | FRD §7.3 | ✅ Selesai (diputuskan) |
| 5 | ~~Harga sewa & seluruh angka monetisasi di PD §8 masih ditandai indikatif, belum divalidasi riset pasar.~~ — **sudah diperbaiki**: harga pay-per-use & subscription final, disetujui tim bisnis 8 Ags 2026 (PD §8, §8.1). Kebijakan refund pay-per-use per sesi tetap final di FRD §7.2; kebijakan refund **subscription** sengaja tetap draft kontekstual (PD §8.2) — bukan gap, karena teks finalnya baru dituntaskan saat halaman Kebijakan & Ketentuan (IA 1.6) dibangun. | ~~Tim Sales/Finance bisa menagih ekspektasi harga final padahal belum ada keputusan bisnis resmi.~~ | PD §8, FRD §7.2 | ✅ Selesai |
| 6 | ~~IA baru mendetailkan content hierarchy untuk 4 dari 28 screen pada sitemap.~~ — **sudah diperbaiki untuk cakupan minimal**: 13 dari 28 screen (46%) kini Done, mencakup seluruh Admin Portal + Subscription + Account Profile sesuai P1-5. 15 screen sisanya (area Public/Guest & alur dasar Booking/Dashboard yang lebih sederhana) tetap pekerjaan lanjutan tim UI/UX, kini tercatat eksplisit di tabel §4.0 (lihat P2-9). | ~~Cakupan kerja desain jauh lebih luas dari yang sudah dirinci; gap tidak ter-track.~~ | IA §4 | ✅ Selesai (cakupan minimal) |

---

## 5. Checklist Siap-Desain (Definition of Ready untuk UI/UX)

| Syarat | Status | Catatan |
| :--- | :---: | :--- |
| Sitemap lengkap & konsisten dengan fitur FRD | ✅ | 28 screen, termasuk cancellation, admin portal & placeholder Kebijakan/Ketentuan |
| RBAC diterjemahkan ke implikasi UI (show/hide/disable) | ✅ | IA §7 |
| Minimal 1 screen kunci per modul utama sudah punya content hierarchy sebagai contoh pola | ✅ | H-01 s/d H-04 |
| Seluruh error state & business rule punya pesan user-facing siap pakai | ✅ | FRD §8 (9 skenario) |
| Semua ID fitur (FR/FEAT) yang disebut di IA punya definisi valid di FRD/PRD | ✅ | FEAT-USR-01/02 kini ditelusuri resmi ke FR-09 (Temuan #3 selesai) |
| Tanggal roadmap & milestone konsisten lintas dokumen | ✅ | Temuan #2 selesai — PRD §9 sudah disamakan dengan PD §9 |
| Harga & kebijakan bisnis (tier, pay-per-use) sudah final | ✅ | Temuan #5 selesai — harga final di PD §8, disetujui 8 Ags 2026. Kebijakan refund subscription (PD §8.2) tetap draft kontekstual secara sengaja, tidak menghalangi desain wireframe halaman 1.6. |

**Kesimpulan:** Tim UI/UX **sudah bisa mulai** wireframing/mockup untuk **13 screen** yang sudah dirinci (Catalog, In-Room Controller, Checkout, Video Vault, Subscription, Account Profile, seluruh Admin Portal) mengikuti pola sitemap + RBAC yang sudah solid — termasuk alur akun/registrasi (1.4, 3.6), harga final (Pricing, Checkout), dan late checkout penalty yang tidak lagi jadi dependency blocking (ditangani manual). Sisa 15 screen (didominasi Public/Guest & alur dasar) tercatat eksplisit di tabel IA §4.0 sebagai pekerjaan lanjutan — tidak ada lagi risiko gap tersembunyi.

---

## 6. Rekomendasi Prioritas & Tracking Perbaikan (Punch List Menuju MVP)

Tabel ini adalah log tindak lanjut yang dapat dilacak (*trace-able*) — setiap baris punya ID tetap, status terkini, dan bukti perbaikan (dokumen + bagian yang berubah) agar progres mudah diaudit dari waktu ke waktu tanpa perlu membaca ulang seluruh riwayat percakapan.

### P0 — Blocker, selesaikan sebelum sprint planning teknis

| ID | Item | Status | Dokumen Terdampak | Bukti / Catatan Perbaikan |
| :--- | :--- | :---: | :--- | :--- |
| P0-1 | Sinkronkan PRD §5 (NFR) & §9 (Milestones) dengan ID dan tanggal yang sudah dipakai FRD/PD (Temuan #1, #2). | ✅ Selesai | PRD →v1.2 (§5, §6, §9, §10 dipulihkan) | NFR-P01-P04/S01-S03/R01-R02 ditambahkan; Success Metrics merujuk ID spesifik; tanggal IoT Smart Lock & Payment Gateway Integration disamakan dengan PD §9; task AI Transcription dipisah dari In-Room Controller. |
| P0-2 | Tambahkan FR resmi untuk User & Access Management di PRD (Temuan #3). | ✅ Selesai | PRD →v1.2 (§3.1, Scope Matrix, §4); FRD →v1.2 (§2, §2.7) | **FR-09 (Manajemen Akun & Autentikasi Pengguna)** ditambahkan di PRD §4; bullet Scope §3.1 & baris Scope Matrix ditambahkan; FRD §2 Feature Module Matrix dan §2.7 dialihkan dari "NFR-S03 (gap PRD)" menjadi FR-09. |
| P0-3 | Putuskan nasib "Late Checkout Penalty" — spesifikasikan sensor occupancy sebagai fitur baru, atau keluarkan rule ini dari cakupan MVP (Temuan #4). | ✅ Selesai | FRD §7.3 | **Keputusan bisnis:** dikeluarkan dari cakupan MVP. Deteksi & penindakan keterlambatan checkout dilakukan **manual oleh petugas Ops** di lokasi (dicatat via panel admin 5.1/5.6), bukan otomatis via sensor. Otomasi sensor occupancy dicatat sebagai opsi peningkatan pasca-MVP, tidak lagi memblokir rilis. |

### P1 — Selesaikan sebelum desain visual final / sebelum coding sprint berjalan penuh

| ID | Item | Status | Dokumen Terdampak | Bukti / Catatan Perbaikan |
| :--- | :--- | :---: | :--- | :--- |
| P1-4 | Validasi harga sewa & kebijakan refund bersama tim bisnis, satukan angkanya di PD §8 dan FRD §7.2 (Temuan #5). | ✅ Selesai | PD →v1.3 (§8, §8.1, §8.2) | **Harga final disetujui** (Senior PM & Finance, 8 Ags 2026): pay-per-use Small/Medium/Large Rp150rb/250rb/350rb per jam; subscription Basic/Pro/Enterprise Rp1,2jt/4,9jt/11,9jt per bulan — dipindahkan dari worksheet §8.1 ke ringkasan §8 utama (inkonsistensi "indikatif" yang sempat tertinggal sudah ditutup). Kebijakan refund subscription (§8.2) **tetap draft kontekstual secara sengaja** — bukan bagian yang harus "selesai", karena teks finalnya menunggu pembangunan halaman Kebijakan & Ketentuan (IA 1.6). Refund pay-per-use per sesi tidak diubah (tetap FRD §7.2). |
| P1-5 | Lanjutkan content hierarchy untuk sisa screen di IA §4 — **cakupan minimal: Admin Portal, Subscription, Account Profile** (Temuan #6). | ✅ Selesai (cakupan minimal) | IA →v1.3 (§4.0, §4.5-§4.13) | Ditambahkan **H-05 s/d H-13** (9 screen: Subscription, Account Profile, dan seluruh 7 screen Admin Portal — Ops Dashboard, Room Management, Hardware Status, Manual Unlock, Audit Logs, Booking & Refund Management, User & Role Management), mengikuti pola H-01-H-04 secara konsisten (route path, breakdown bernomor, rujukan FR/FEAT/error code, catatan akses RBAC per screen). Tabel §4.0 diperbarui jadi 13/28 Done. **Satu catatan jujur ditambahkan** di H-11 (Audit Logs): FRD §4 RBAC belum eksplisit mendefinisikan permission untuk log audit — ditandai sebagai asumsi kerja terbuka, bukan diklaim sebagai fakta pasti, agar tidak menimbulkan inkonsistensi baru. Sisa 15 screen (area Public/Guest & alur dasar) dilanjutkan sebagai P2-9 di bawah. |
| P1-6 | Lengkapi kontrak API yang baru disebut ringkas di FRD §5.5 tanpa contoh payload penuh (`/subscriptions/{user_id}/quota`, `/bookings/{id}/vault`). | ⏳ Ditunda (sengaja) | FRD →v1.3 (§5.5) | **Ditunda dengan sengaja atas arahan:** akan dilengkapi saat tim masuk sesi desain backend, bukan pada iterasi dokumen ini. Catatan penundaan eksplisit sudah ditambahkan di FRD §5.5 agar tidak terlupa. |
| P1-7 | Sinkronkan tipe data & deklarasi FK kolom `wallet_transactions.reference_id` antara `README/3-DBSchema.md` (dictionary §3.18 + ERD: `VARCHAR(100)`, tanpa FK eksplisit) dan `README/3-DBQuery.md` (DDL aktual: `UUID` + `CONSTRAINT fk_wallet_reference FOREIGN KEY ... REFERENCES booking_cancellations (id)`). | ⏳ Belum dimulai | `README/3-DBSchema.md` §3.18 + ERD; `README/3-DBQuery.md` §3.5 | Ditemukan saat cross-check siklus 7 (`docs/X_ProgressSummary.md` §10, temuan #1). `booking_cancellations.id` adalah `UUID`, sehingga versi `UUID` di DBQuery secara teknis benar dan DBSchema yang perlu direvisi ke atas (`VARCHAR(100)` → `UUID`, tambahkan baris FK & relasi ERD `booking_cancellations \|\|--o{ wallet_transactions`) — bukan sebaliknya. Sengaja tidak diperbaiki sepihak pada siklus ini karena diminta hanya reporting. |
| P1-8 | Selesaikan nilai `reason: "ROOM_MISMATCH"` pada payload MQTT `/classroom/door/unlock` (`README/4-APIDesign.md` §6.2) — nilai ini **tidak ada** di `door_access_result_enum` (`README/3-DBSchema.md` §4 / `README/3-DBQuery.md` §3.3, hanya `SUCCESS`/`TOKEN_EXPIRED_OR_INVALID`) maupun matriks 9 kode error FRD §8. | ⏳ Belum dimulai | `README/4-APIDesign.md` §6.2; `README/3-DBSchema.md` §3.19/§4; `README/3-DBQuery.md` §3.3; FRD §8 | Ditemukan saat cross-check siklus 8 (`docs/X_ProgressSummary.md` §11, temuan #1). Alur `README/3-DBSchema.md` §5.7 secara eksplisit meng-insert nilai `reason`/`result` ini ke kolom `door_access_attempts.result` yang dibatasi `CHECK` constraint 2 nilai — jika `ROOM_MISMATCH` benar-benar dikirim backend apa adanya, `INSERT` akan gagal. Perlu keputusan: tambahkan `ROOM_MISMATCH` sebagai nilai enum resmi (di DBSchema, DBQuery, **dan** matriks error FRD §8) jika skenario ini nyata dibutuhkan, atau hapus dari payload MQTT dan gunakan `TOKEN_EXPIRED_OR_INVALID` generik. |
| P1-9 | Putuskan status fitur add-on `live_streaming` yang muncul di `README/4-APIDesign.md` §7 (skema validasi `add_ons`) dan §3.1 (contoh payload `POST /bookings/hold`) — tidak punya definisi FR/FEAT, aturan bisnis (harga, workflow), baris di IA Checkout (H-03), maupun kolom database apa pun. Satu-satunya sumber adalah sebutan sekilas "Live Streaming Multi-platform" di PD §8 sebagai item roadmap bisnis, bukan spesifikasi fitur siap-implementasi. | ⏳ Belum dimulai | `README/4-APIDesign.md` §3.1, §7; PD §8; FRD (Data Validation Rules); IA §4.3 (H-03) | Ditemukan saat cross-check siklus 8 (`docs/X_ProgressSummary.md` §11, temuan #2). Berbeda dari `ai_transcription` yang sudah punya kolom `bookings.is_ai_transcription_addon`, alur harga, dan baris breakdown di IA H-03, `live_streaming` tidak punya padanan apa pun di FRD/DB/IA — berisiko backend mem-validasi & menerima input yang tidak bisa diproses lebih lanjut. Perlu keputusan: masukkan ke scope MVP dengan spesifikasi lengkap di FRD+DBSchema+IA, atau keluarkan dari `add_ons` enum `4-APIDesign.md` hingga fiturnya benar-benar didefinisikan. |

### P2 — Bisa berjalan paralel, tidak memblokir MVP

| ID | Item | Status | Dokumen Terdampak | Bukti / Catatan Perbaikan |
| :--- | :--- | :---: | :--- | :--- |
| P2-7 | Lengkapi Problem Statement PD §3.1 dengan data riset pasar kuantitatif (saat ini kualitatif). | ⏳ Belum dimulai | PD §3.1 | — |
| P2-8 | Evaluasi apakah roadmap kuartalan (PD §9) dan milestone sprint (PRD §9) tetap dua dokumen terpisah atau dikonsolidasi, mengikuti opsi anti-redundansi di PRD §10.3. | ⏳ Belum dimulai | PD §9, PRD §9-§10 | — |
| P2-9 | Lengkapi content hierarchy untuk 15 screen sisanya di IA §4.0 (area Public/Guest: Landing, Pricing, Auth, Help, Kebijakan & Ketentuan; alur dasar Booking/Dashboard: Room Detail, Schedule Picker, Success Page, Overview/Home, Active Bookings, E-Ticket, Cancellation Confirmation; In-Room: Room Auth Gate, Studio Live Feed, Session Complete). | ⏳ Belum dimulai | IA §4.0 | Di luar cakupan minimal P1-5; diturunkan prioritasnya ke P2 karena polanya sudah jelas (mengikuti H-01 s/d H-13) dan tidak memblokir mulainya desain visual untuk 13 screen yang sudah Done. |
| P2-10 | Konfirmasi & tambahkan baris permission eksplisit untuk **Audit Logs (5.5)** di FRD §4 RBAC — ditemukan saat cross-check final bahwa tabel RBAC tidak punya baris untuk fitur ini. | ⏳ Belum dimulai | FRD §4 | Ditemukan saat menulis H-11 (IA §4.11). Untuk sementara H-11 memakai asumsi kerja (Super Admin Full Access, Studio Admin tanpa akses) yang ditandai eksplisit sebagai catatan terbuka, **bukan diklaim sebagai fakta RBAC resmi** — sengaja tidak diperbaiki sepihak di FRD karena ini keputusan produk/keamanan yang butuh sign-off, bukan sekadar koreksi editorial. **Catatan tambahan (siklus 8):** `README/4-APIDesign.md` §5.6 & §10 mengunci endpoint `/admin/audit-logs` ke `SUPER_ADMIN`-saja dan mengklaim status **"Selesai"** untuk P2-10 — namun FRD §4 dikonfirmasi ulang masih **belum** punya baris RBAC resmi untuk Audit Logs. Status kanonis P2-10 di tabel ini **tetap ⏳ Belum dimulai**; klaim "Selesai" di `4-APIDesign.md` §10 premature dan perlu dikoreksi mengikuti FRD, bukan sebaliknya (lihat `docs/X_ProgressSummary.md` §11 temuan #6). |
| P2-11 | Quick Link "Manajemen Ruangan" di prototipe `admin/dashboard.html` (H-07) masih mengarah ke `href="#"`, padahal prototipe H-08 (`admin/rooms.html`) **sudah ada** di repo dan seharusnya sudah bisa ditautkan langsung. | ⏳ Belum dimulai | Prototipe `admin/dashboard.html` (bukan dokumen sumber) | Ditemukan saat cross-check prototipe Tahap 3 vs IA §4.7. Quick Link lain (Hardware/Manual Unlock/Booking&Refund/User&Role) wajar tetap `#` karena H-09/H-10/H-12/H-13 memang belum diprototipekan. |
| P2-12 | Repo memiliki dua stack front-end yang tumpang tindih: scaffold React+Vite+Tailwind4 (`package.json`, `vite.config.ts`, `src/App.tsx` — `App.tsx` masih kosong/tidak dipakai) di samping prototipe HTML statis + Tailwind CDN yang aktual dipakai (`index.html`, `in-room.html`, dll). Ada juga dua lockfile sekaligus (`bun.lock` kosong & `package-lock.json`). | ⏳ Belum dimulai | Workspace root (bukan dokumen sumber) | Berisiko membingungkan kontributor baru (`npm run dev` merender halaman kosong, bukan prototipe). Perlu keputusan: scaffold React dipertahankan untuk tahap Antigravity nanti (dan didokumentasikan sebagai demikian), atau dihapus jika belum relevan di Tahap 3. Bukan gap dokumen sumber, dicatat di sini agar tidak hilang. |
| P2-13 | ~~`docs/GEMINI_SYSTEM_PROMPT.md` §"Fase Proyek Saat Ini" masih menandai Prototyping (Google AI Studio) sebagai "⬜ Berikutnya", padahal secara faktual 5 prototipe HTML sudah dibuat di branch `2-design`.~~ — **sudah diperbaiki** (siklus 9). | ✅ Selesai | `docs/GEMINI_SYSTEM_PROMPT.md` →v1.1 | Tabel "Fase Proyek Saat Ini" diperbarui: Prototyping & Database/System Design ditandai ✅ Selesai, Implementasi API Backend ditandai 🏃 Aktif, Desain UI/UX ditandai ⚠️ digabung ke Prototyping. Sekaligus menambahkan Tahap 5 baru (Implementasi API Backend) & subsection panduan `🔌` di dokumen tersebut. |
| P2-14 | `subscription_plans.storage_gb` (skema DB) tidak punya angka GB pasti untuk tier Basic ("Standard Storage") maupun Enterprise — PD §8.1.B hanya memberi angka eksplisit untuk Pro (500GB). | ⏳ Belum dimulai | PD §8.1.B; `README/3-DBSchema.md` §3.2 | Kolom sudah dibuat *nullable* di skema (bukan angka karangan) sebagai mitigasi teknis sementara — keputusan angka final tetap perlu sign-off tim bisnis sebelum go-live. |
| P2-15 | Skema DB (`README/3-DBSchema.md`) menambahkan `users.wallet_balance` (saldo kredit dari refund pembatalan booking, sesuai FRD §7.2) tapi belum ada keputusan bisnis soal nasibnya saat pengguna menghapus akun (dicairkan/hangus/klaim manual). | ⏳ Belum dimulai | `README/3-DBSchema.md` §8 | Rekomendasi teknis sementara: blokir soft delete jika saldo > 0. Perlu sign-off produk/finance, bukan keputusan final sepihak dari desain skema. |
| P2-16 | Alur refund subscription (`README/3-DBSchema.md` §5.6, mengacu PD §8.2 draft kontekstual) belum punya SLA hari kerja proses refund maupun mekanisme pencairan dana (manual transfer vs reversal otomatis PG) yang final. | ⏳ Belum dimulai | PD §8.2; `README/3-DBSchema.md` §5.6 | Sama seperti kebijakan refund subscription itu sendiri (lihat P1-4), poin ini menunggu teks final halaman Kebijakan & Ketentuan (IA 1.6) sebelum diimplementasikan. |
| P2-17 | Tambahkan `CONSTRAINT chk_recording_dates CHECK (stopped_at IS NULL OR started_at IS NULL OR stopped_at > started_at)` ke `CREATE TABLE recordings` di `README/3-DBQuery.md` §3.6 — DBSchema §3.10 sudah mendokumentasikan aturan `> started_at` untuk `stopped_at`, tapi constraint-nya tidak ada di DDL aktual. | ⏳ Belum dimulai | `README/3-DBQuery.md` §3.6; `README/3-DBSchema.md` §3.10 | Ditemukan saat cross-check siklus 7 (`docs/X_ProgressSummary.md` §10, temuan #2). Pola yang sama sudah benar diimplementasikan untuk tabel sejenis (`room_maintenance_logs.ended_at`, `chk_maintenance_dates`) — tinggal menyalin pola yang sama ke `recordings`. |
| P2-18 | Putuskan & dokumentasikan secara eksplisit di `README/3-DBSchema.md`: 6 aturan bisnis di kolom "Batasan & Aturan Bisnis" (format email `users.email`, panjang min. `users.name`, jendela jam `bookings.start_time`/`end_time` 07:00-22:00 & kelipatan 30 menit, kelipatan 0.5 jam `user_subscriptions.quota_hours_used`, batas 500 karakter `subscription_refunds.reason`/`booking_cancellations.reason`) mana yang wajib jadi `CHECK` constraint di `README/3-DBQuery.md` dan mana yang sengaja diserahkan ke validasi level aplikasi. | ⏳ Belum dimulai | `README/3-DBSchema.md` (kolom "Batasan & Aturan Bisnis / FK" di beberapa tabel §3); `README/3-DBQuery.md` | Ditemukan saat cross-check siklus 7 (`docs/X_ProgressSummary.md` §10, temuan #3). Bukan berarti keenamnya salah/hilang — bisa jadi sengaja app-layer — tapi kolom "Batasan & Aturan Bisnis / FK" DBSchema tidak membedakan mana yang DB-enforced vs app-layer, sehingga ambigu bagi siapa pun yang mengimplementasikan ulang dari dokumen skema saja. Perlu keputusan desain, bukan koreksi editorial sepihak. |
| P2-19 | Lengkapi `GET /rooms` (`README/4-APIDesign.md` §2.1): tambahkan query parameter untuk 2 dari 4 filter yang didokumentasikan IA §4.1 (kapasitas & rentang harga per jam — saat ini hanya `location`, `facility`, `date`); revisi skema respons agar 1 ruangan mengembalikan `capacity_tier` + 1 `price_per_hour` miliknya sendiri (selaras `README/3-DBSchema.md` §3.5), bukan objek `hourly_rates` berisi ketiga tarif SMALL/MEDIUM/LARGE sekaligus. | ⏳ Belum dimulai | `README/4-APIDesign.md` §2.1; IA §4.1 (H-01); `README/3-DBSchema.md` §3.5 | Ditemukan saat cross-check siklus 8 (`docs/X_ProgressSummary.md` §11, temuan #3). FRD §5.5 hanya mendefinisikan endpoint ini secara ringkas (path + 3 query param, tanpa skema respons), jadi `4-APIDesign.md` bebas melengkapi detail — tapi pelengkapannya perlu selaras ke model data 1 ruangan = 1 tier di DB dan ke filter yang sudah dijanjikan IA, bukan menciptakan bentuk baru yang berbeda dari keduanya. |
| P2-20 | Samakan istilah status pada `GET /bookings/{id}/vault` (`README/4-APIDesign.md` §4.2): field `recording.status`/`transcription.status` memakai `COMPLETED`, padahal `README/3-DBSchema.md` menyimpan `READY` untuk kedua kolom (`recordings.status` §3.10, `ai_transcriptions.status` §3.11). | ⏳ Belum dimulai | `README/4-APIDesign.md` §4.2; `README/3-DBSchema.md` §3.10, §3.11 | Ditemukan saat cross-check siklus 8 (`docs/X_ProgressSummary.md` §11, temuan #4). Boleh saja API sengaja memakai istilah publik berbeda dari nilai internal DB (`READY` → `COMPLETED`), tapi perlu dicatat eksplisit sebagai pemetaan/terjemahan di salah satu dokumen agar implementasi tidak salah mengira keduanya adalah nilai yang sama persis. |
| P2-21 | Lengkapi daftar opsi `payment_method` pada komentar `POST /payments/charge` (`README/4-APIDesign.md` §3.2) — saat ini hanya menyebut 4 dari 7 nilai `payment_method_enum` (`README/3-DBSchema.md` §4 / `README/3-DBQuery.md` §3.1): `VA_BNI` dan `EWALLET_SHOPEEPAY` hilang dari komentar contoh (`SUBSCRIPTION_QUOTA` wajar tidak disebut karena dipilih sistem, bukan pengguna, di alur §5.2). | ⏳ Belum dimulai | `README/4-APIDesign.md` §3.2; `README/3-DBSchema.md` §4 | Ditemukan saat cross-check siklus 8 (`docs/X_ProgressSummary.md` §11, temuan #5). Risiko rendah (hanya komentar contoh, bukan validasi terikat), tapi berpotensi membuat tim frontend melewatkan 2 metode pembayaran VA/E-Wallet yang sebenarnya valid saat membangun UI pemilihan metode bayar. |
| P2-22 | Putuskan skema penyimpanan preferensi notifikasi (`profile.html` / H-06, 3 toggle: konfirmasi booking, status vault, tagihan) sebelum `GET/PUT /users/me/notifications` bisa diimplementasikan. | ⏳ Belum dimulai | `README/3-DBSchema.md` §3.1 (`users`) | Ditemukan saat cross-check UI vs skema (`README/4-APIBackend.md` §4.4, siklus 10). Bukan gap dokumen API saja — **tidak ada kolom/tabel pendukung sama sekali** di `3-DBSchema.md`. Opsi: kolom `users.notification_preferences JSONB`, atau tabel terpisah. Endpoint ini sengaja **tidak diimplementasikan** di `server/routes/users.routes.js` sampai keputusan ini diambil. |
| P2-23 | Putuskan mekanisme "Keluar Semua Perangkat" (`profile.html` / H-06) — `README/4-APIDesign.md` §2.4 secara eksplisit mendesain logout sebagai **JWT stateless, revokasi lokal saja tanpa blacklist server**, sehingga server tidak punya cara nyata memaksa logout perangkat lain sebelum token kedaluwarsa (maks. 24 jam). | ⏳ Belum dimulai | `README/4-APIDesign.md` §2.4 | Ditemukan saat cross-check UI vs skema (`README/4-APIBackend.md` §4.4, siklus 10). Perlu keputusan: terima keterbatasan ini (tombol UI jadi kosmetik/no-op fungsional), atau tambahkan token blacklist (Redis) sebagai perubahan arsitektur — ini kontradiksi langsung dengan desain §2.4 yang sudah ada, bukan sekadar endpoint yang belum ditulis. `POST /auth/logout-all` sengaja **tidak diimplementasikan** di `server/routes/auth.routes.js` sampai keputusan ini diambil. |

**Ringkasan status:** Seluruh 3 blocker P0 **sudah selesai** (P0-1, P0-2, P0-3). Seluruh 3 item P1 lama juga **sudah selesai untuk cakupan yang diminta**: P1-4 (harga final + draft kebijakan refund kontekstual) dan P1-5 (13/28 screen Done, mencakup seluruh cakupan minimal Admin Portal/Subscription/Account Profile), sementara P1-6 **sengaja ditunda** ke sesi desain backend sesuai arahan (dan kini sudah dijawab isinya oleh `README/4-APIDesign.md` §3.5/§4.2 — lihat siklus 8). **P1-7 (siklus 7)** — inkonsistensi tipe data `wallet_transactions.reference_id` antar `3-DBSchema.md` dan `3-DBQuery.md` — belum dikerjakan. **P1-8 & P1-9 (baru, siklus 8)** — nilai MQTT `ROOM_MISMATCH` yang tidak backed DB/FRD, dan add-on `live_streaming` yang tidak backed FR/FEAT/DB — keduanya belum dikerjakan. Cross-check final (siklus 4) tidak menemukan ID yatim atau drift tanggal/harga, tapi menemukan & langsung memperbaiki 2 inkonsistensi penamaan/kelengkapan di IA, plus mencatat 1 gap RBAC baru (P2-10) yang sengaja tidak diputuskan sepihak. Cross-check siklus 5 (awal Tahap 3 Prototyping) menambahkan 3 temuan operasional baru (P2-11 s/d P2-13) di prototipe/workspace — bukan di dokumen sumber. Cross-check siklus 6 (analisa Skema Database, Tahap 4) memperbaiki 1 bug logika & 3 gap struktural langsung di `README/3-DBSchema.md` (detail: §9), serta menambahkan 3 keputusan bisnis terbuka baru (P2-14 s/d P2-16) yang sengaja tidak diputuskan sepihak. Cross-check siklus 7 (`3-DBQuery.md` vs `3-DBSchema.md`, detail: §10) menambahkan **P1-7, P2-17, P2-18**. Cross-check siklus 8 (`4-APIDesign.md` vs FRD/PRD/IA/DBSchema/DBQuery, detail: §11) menambahkan **P1-8, P1-9, P2-19 s/d P2-21**, plus catatan koreksi pada **P2-10** (klaim "Selesai" di `4-APIDesign.md` premature, status kanonis tetap terbuka). Seluruhnya dilaporkan saja tanpa perbaikan sepihak, sesuai arahan. P2-9 s/d P2-21 belum dikerjakan pada siklus ini — **tidak memblokir kelanjutan prototyping, desain skema, eksekusi DDL, maupun desain API** yang sudah Done.

---

## 7. Indeks ID Lintas Dokumen (Referensi Cepat)

| Kategori ID | Contoh | Didefinisikan di |
| :--- | :--- | :--- |
| Problem | P1-P3 | PD §3.1 |
| JTBD | JTBD 1-3 | PD §5 |
| Functional Requirement | FR-01 s/d FR-09 (FR-09 baru: Manajemen Akun & Autentikasi) | PRD §4 |
| Non-Functional Requirement | NFR-P01-P04, NFR-S01-S03, NFR-R01-R02 | PRD §5 |
| Acceptance Criteria | AC-1.1 s/d AC-3.2 | PRD §7 |
| Risiko Produk | R-01 s/d R-03 | PRD §8.1 |
| Fitur Teknis | FEAT-CAT/BKG/PAY/IOT/CTL/VLT/USR-xx | FRD §2 |
| Kode Error | SLOT_ALREADY_LOCKED, PAYMENT_TIMEOUT, IOT_GATEWAY_OFFLINE, TOKEN_EXPIRED_OR_INVALID, MAX_CONCURRENT_LOCKS_EXCEEDED, QUOTA_EXCEEDED, CANCELLATION_NOT_ALLOWED, TRANSCRIPTION_FAILED, CLOUD_UPLOAD_FAILED | FRD §8 |
| Screen | 1.1-1.6, 2.1-2.4, 3.1-3.7, 4.1-4.4, 5.1-5.7 (28 total) | IA §2 |
| Screen dengan Content Hierarchy | H-01 s/d H-13 (13 dari 28 screen) | IA §4 |

---

## 8. Tracking Progres Tahap 3 — Prototyping (Google AI Studio)

> **Catatan cakupan:** Tabel ini melacak progres **prototipe kode** (HTML/React, di luar 4 dokumen sumber), bukan dokumen. Ditambahkan siklus 5 agar progres Tahap 3 tidak lagi hanya tersirat dari isi repo, mengikuti prinsip yang sama dengan tabel Cakupan Screen Hierarchy di IA §4.0 (anti-berulangnya Temuan #6).

| Screen (H-xx) | File Prototipe | Status | Catatan |
| :--- | :--- | :---: | :--- |
| H-01 Catalog Search | `index.html` | ✅ Done | Termasuk simulasi Empty State & `SLOT_ALREADY_LOCKED`. |
| H-02 In-Room Control Panel | `in-room.html` | ✅ Done | Termasuk simulasi timer & `IOT_GATEWAY_OFFLINE`. |
| H-03 Checkout & Payment | — | ⏳ Belum dimulai | — |
| H-04 Cloud Video Vault | — | ⏳ Belum dimulai | — |
| H-05 Subscriptions | — | ⏳ Belum dimulai | — |
| H-06 Account Profile | `profile.html` | ✅ Done | Termasuk toggle simulasi role Member/Premium untuk uji RBAC IA §7. |
| H-07 Ops Dashboard | `admin/dashboard.html` | ✅ Done* | *Menggabungkan juga modul "Catat Biaya Late Checkout Manual" yang menurut IA §4.12 milik H-12 (Booking & Refund Management), bukan H-07 — perlu dipisah saat H-12 dibangun sebagai screen sendiri, atau didokumentasikan sebagai keputusan desain sengaja. Quick Link internal lihat P2-11. |
| H-08 Room Management | `admin/rooms.html` | ✅ Done | — |
| H-09 Hardware Status | — | ⏳ Belum dimulai | — |
| H-10 Manual Unlock | — | ⏳ Belum dimulai | — |
| H-11 Audit Logs | — | ⏳ Belum dimulai | Tunggu P2-10 (RBAC Audit Logs) sebelum difinalisasi. |
| H-12 Booking & Refund Management | — | ⏳ Belum dimulai | Lihat catatan H-07 di atas — sebagian kontennya sudah "bocor" ke dashboard. |
| H-13 User & Role Management | — | ⏳ Belum dimulai | — |

**Ringkasan:** 5 dari 13 screen Done (H-01–H-13) sudah punya prototipe (38%). Tidak ditemukan penyimpangan istilah/harga/kode error terhadap dokumen sumber pada 5 prototipe yang ada. 3 temuan operasional dicatat sebagai P2-11 s/d P2-13 di §6.

---

## 9. Tracking Temuan & Perbaikan Skema Database (Tahap 4 — Google Antigravity)

> **Catatan cakupan:** Bagian ini adalah **satu-satunya tempat** narasi lengkap analisa `README/3-DBSchema.md` disimpan — dokumen skema itu sendiri sengaja hanya menyimpan hasil perbaikan (desain final) plus pointer balik ke sini, agar tidak ada laporan analisis yang terduplikasi di dua tempat. `README/3-DBSchema.md` naik dari **v1.0 ke v1.1** pada siklus ini (lihat Riwayat Revisi di header dokumen tersebut untuk ringkasan per-bagian).

| # | Temuan | Perbaikan yang Diterapkan | Status | Bagian Terdampak (`README/3-DBSchema.md`) |
| :--- | :--- | :--- | :---: | :--- |
| 1 | Tautan "Dokumen Terkait" ke `README/UseCaseDiagram.md` rusak (file sebenarnya `README/2-UseCaseDiagram.md` pasca-reorganisasi folder `README/`); `docs/X_ProgressSummary.md` & `README/2-UITesting.md` tidak dirujuk sama sekali. | Tautan diperbaiki; 2 dokumen terkait ditambahkan. | ✅ Selesai | Header |
| 2 | Deskripsi `users.role` & `audit_logs.actor_role` memakai istilah informal "Admin/Super" yang tidak cocok dengan `role_enum` resmi (`STUDIO_ADMIN`/`SUPER_ADMIN`) yang didefinisikan di §4 dokumen yang sama — inkonsistensi internal. | Deskripsi disamakan persis dengan `role_enum`. | ✅ Selesai | §3.1, §3.16 |
| 3 | **Bug logika:** `monthly_hours_quota` Enterprise memakai dua sentinel berbeda sekaligus ("-1 / 9999") tanpa definisi tunggal; pseudocode alur pemotongan kuota langsung mengurangkan tanpa menangani kasus "Unlimited", sehingga `sisa_kuota` akan selalu negatif untuk Enterprise dan **salah memicu `QUOTA_EXCEEDED`** pada pelanggan yang seharusnya tanpa batas. | Sentinel dikunci ke `-1` saja; pseudocode ditambahkan pengecekan eksplisit (`IF quota_hours_total = -1 THEN skip`) sebelum pengurangan kuota. | ✅ Selesai | §3.2, §5.2 |
| 4 | `subscription_plans.storage_gb` mewajibkan angka pasti untuk semua tier, padahal PD §8.1.B hanya memberi angka eksplisit untuk Pro (500GB) — Basic ("Standard Storage") dan Enterprise tidak disebutkan sama sekali, sehingga angka Basic/Enterprise sebelumnya berisiko jadi karangan. | Kolom dibuat *nullable* + ditandai ⚠️ Asumsi (mitigasi teknis). Angka final tetap perlu keputusan bisnis — dilacak sebagai **P2-14** di §6. | ✅ Selesai (mitigasi teknis) | §3.2 |
| 5 | Tidak ada tabel untuk refund pembatalan **booking pay-per-use self-service** (FEAT-BKG-03) — padahal sudah punya kontrak API eksplisit (`POST /api/v1/bookings/{id}/cancel`, FRD §5.4, response memuat `refund_percentage`/`refund_amount`) dan 2 screen IA (3.7, 5.6). Tabel `subscription_refunds` yang ada hanya untuk level langganan. | Ditambahkan tabel `booking_cancellations` (field disamakan persis dengan payload API §5.4) + narasi alur baru §5.5 + relasi ERD + indeks pendukung. | ✅ Selesai | §3.17, §5.5, ERD, §6.5 |
| 6 | FRD §7.2 menyebut eksplisit refund pembatalan booking "dikreditkan ke **saldo wallet**/kuota" — istilah "wallet" ini tidak direpresentasikan sama sekali di draf skema v1.0 (hanya kuota subscription yang ada). | Ditambahkan kolom `users.wallet_balance` + tabel ledger append-only `wallet_transactions` + logika kredit saldo di alur §5.5. | ✅ Selesai | §3.1, §3.18, §5.5, ERD, §6.5 |
| 7 | Matriks error FRD §8 mensyaratkan deteksi ">3x percobaan gagal dalam 10 menit" untuk `TOKEN_EXPIRED_OR_INVALID`, dan IA §4.7 (H-07) mensyaratkan **Live Alert Feed** yang menampilkannya — namun skema v1.0 hanya mencatat token yang **valid** (`room_access_tokens`), tidak ada jejak percobaan gagal. | Ditambahkan tabel `door_access_attempts` (mencatat semua percobaan, sukses & gagal) + narasi alur deteksi ambang batas §5.7 + indeks pendukung. | ✅ Selesai | §3.19, §5.7, ERD, §6.5 |
| 8 | Tabel `subscription_refunds` sudah ada sejak v1.0, tapi tidak pernah punya narasi alur (workflow) — gap konsistensi internal dokumen (tabel ada, prosesnya tidak dijelaskan). | Ditambahkan narasi alur §5.6 mengacu PD §8.2, dengan ⚠️ Asumsi eksplisit untuk SLA/mekanisme pencairan dana yang belum final — dilacak sebagai **P2-16** di §6. | ✅ Selesai | §5.6 |
| 9 | Tidak ada ketentuan soal `users.wallet_balance` (baru ditambahkan) saat akun pengguna dihapus (soft delete, §8 dokumen skema). | Ditambahkan catatan ⚠️ Asumsi + rekomendasi teknis minimal (blokir soft delete jika saldo > 0) — keputusan final dilacak sebagai **P2-15** di §6. | ✅ Selesai (mitigasi teknis) | §8 |

**Verifikasi silang ke source code:** field yang dirujuk skema dari prototipe (`inputNama`, `inputPhone`, `inputInstitusi`, `toggle2FA` di `profile.html`; `maintenanceNotes` di `admin/rooms.html`; `overrideReason`, `lateCheckoutNotes` di `admin/dashboard.html`) **seluruhnya cocok** dengan `id` elemen HTML aktual — tidak ada perbaikan diperlukan di sisi ini.

**Ringkasan:** 9 temuan diperbaiki langsung pada siklus ini (1 bug logika, 2 inkonsistensi istilah, 1 tautan rusak, 3 tabel baru + alurnya, 1 workflow yang belum terdokumentasi, 1 kebijakan soft-delete). 3 keputusan bisnis yang sengaja tidak diputuskan sepihak dilacak sebagai P2-14 s/d P2-16 di §6.

---

## 10. Tracking Temuan Cross-Check: `README/3-DBQuery.md` vs `README/3-DBSchema.md` (Tahap 4 lanjutan — Google Antigravity)

> **Catatan cakupan:** Bagian ini adalah narasi lengkap analisa `README/3-DBQuery.md` (kueri DDL inisialisasi database) terhadap `README/3-DBSchema.md` v1.1 (skema acuannya sendiri, dirujuk langsung di header DBQuery). Berbeda dari siklus 6 (§9), **tidak ada perbaikan yang diterapkan langsung** ke kedua dokumen pada siklus ini — sesuai permintaan, temuan hanya dilaporkan di sini dan didaftarkan sebagai backlog baru di §6 (P1-7, P2-17, P2-18).

### 10.1 Metodologi

Seluruh 19 `CREATE TABLE` di DBQuery dicocokkan satu-satu terhadap tabel padanannya di kamus data DBSchema §3 (tipe data, nullability, default, `CHECK`, dan `FOREIGN KEY` beserta aksi `ON DELETE`), diikuti pengecekan urutan dependensi FK (agar tidak ada tabel dibuat sebelum tabel yang dirujuknya) dan pencocokan seluruh 11 indeks fisik (DBSchema §6 vs DBQuery §4).

### 10.2 Hasil yang Konsisten (Tidak Perlu Tindakan)

| Aspek | Hasil Pengecekan |
| :--- | :--- |
| Urutan pembuatan tabel vs dependensi FK | ✅ Konsisten — tidak ada tabel yang dibuat sebelum tabel yang dirujuk FK-nya (termasuk 3 tabel baru siklus 6: `booking_cancellations`, `wallet_transactions`, `door_access_attempts`, yang urutannya benar di §2.4→§3.4→§3.5). |
| Tipe data & nullability seluruh kolom (selain 1 pengecualian di §10.3 #1) | ✅ Konsisten 1:1 dengan kamus data DBSchema §3.1-§3.19. |
| Nilai `CHECK` untuk seluruh kolom status/enum (role, status booking/payment/subscription/refund, facility, device type, dsb.) | ✅ Konsisten 1:1 dengan `role_enum` s/d `door_access_result_enum` di DBSchema §4. |
| Aksi `ON DELETE` (CASCADE/RESTRICT/SET NULL) pada seluruh FK yang eksplisit disebut DBSchema | ✅ Konsisten — termasuk pola FK ke `users` yang seragam `RESTRICT` untuk menjaga jejak audit historis. |
| 11 indeks fisik (§6.1-§6.5 DBSchema vs §4.1-§4.6 DBQuery) | ✅ Seluruhnya ada, tidak ada yang hilang atau berbeda kolom — hanya pengelompokan sub-bagian yang sedikit berbeda (kosmetik, bukan gap fungsional). |
| Versi PostgreSQL (`16+` di DBSchema §1 vs `17` di header DBQuery) | ✅ Tidak kontradiktif — 17 adalah himpunan bagian dari "16+", sengaja tidak dicatat sebagai temuan. |
| Field yang dirujuk skema dari prototipe UI (`inputNama`, `maintenanceNotes`, dst.) | ✅ Sudah diverifikasi valid di siklus 6 (§9), tidak diulang di sini. |

### 10.3 Temuan

| # | Temuan | Rekomendasi | Status | Bagian Terdampak |
| :--- | :--- | :--- | :---: | :--- |
| 1 | **Inkonsistensi tipe data + FK tersembunyi:** DBSchema §3.18 (kamus data) dan ERD (§2, entitas `wallet_transactions`) menyatakan `reference_id` bertipe `VARCHAR(100)` tanpa deklarasi `FOREIGN KEY` eksplisit — hanya narasi "Merujuk `booking_cancellations.id`". DBQuery §3.5 justru mengimplementasikannya sebagai `UUID` dengan `CONSTRAINT fk_wallet_reference FOREIGN KEY (reference_id) REFERENCES booking_cancellations (id) ON DELETE SET NULL`. Karena `booking_cancellations.id` adalah `UUID` (§3.17), versi DBQuery **secara teknis benar**; DBSchema-lah yang perlu naik versi. ERD DBSchema juga tidak punya baris relasi `booking_cancellations \|\|--o{ wallet_transactions`, padahal relasi ini nyata ada di DDL. | Naikkan DBSchema §3.18 & ERD: ubah tipe `reference_id` ke `UUID`, tambahkan baris "`FOREIGN KEY` -> `booking_cancellations.id`" secara eksplisit di kolom Batasan/FK, dan tambahkan 1 baris relasi ERD yang hilang. | ⏳ Dilaporkan (backlog **P1-7**) | DBSchema §3.18, ERD (§2); DBQuery §3.5 |
| 2 | **Constraint terdokumentasi tapi tidak diimplementasikan:** DBSchema §3.10 mensyaratkan `stopped_at` pada tabel `recordings` bernilai `> started_at`, tapi `CREATE TABLE recordings` di DBQuery §3.6 hanya punya `chk_recording_status` dan `chk_recording_duration` — tidak ada CHECK untuk relasi `stopped_at`/`started_at`. Pola yang identik justru sudah benar diimplementasikan untuk tabel lain di dokumen yang sama (`room_maintenance_logs.ended_at`, via `chk_maintenance_dates CHECK (ended_at IS NULL OR ended_at > started_at)`), sehingga ini kemungkinan besar celah copy-paste, bukan keputusan desain sengaja. | Tambahkan `CONSTRAINT chk_recording_dates CHECK (stopped_at IS NULL OR started_at IS NULL OR stopped_at > started_at)` ke DDL `recordings`, mengikuti pola `chk_maintenance_dates`. | ⏳ Dilaporkan (backlog **P2-17**) | DBQuery §3.6; DBSchema §3.10 |
| 3 | **Aturan bisnis di kamus data tanpa CHECK padanan (ambiguitas app-layer vs DB-layer):** 6 baris "Batasan & Aturan Bisnis" di DBSchema §3 tidak punya `CHECK` constraint padanan di DBQuery — (a) `users.email` "format email valid" (§3.1), (b) `users.name` "Min. 2 karakter" (§3.1), (c) `bookings.start_time` jendela `07:00-22:00` (§3.7), (d) `bookings.end_time` "minimal selisih 60 menit & kelipatan 30m" — DBQuery hanya punya `chk_booking_times CHECK (end_time > start_time)` (§3.7 vs DBQuery §2.4), (e) `user_subscriptions.quota_hours_used` "kelipatan 0.5 jam" (§3.3), (f) `subscription_refunds.reason` & `booking_cancellations.reason` "Max 500 karakter" (§3.4, §3.17) — kolomnya `TEXT` tanpa batas panjang di DDL. Tidak otomatis berarti bug — bisa jadi keenamnya sengaja diserahkan ke validasi level aplikasi/API — tapi kolom "Batasan & Aturan Bisnis / FK" DBSchema mencampur aturan yang sudah jadi CHECK (mis. `chk_user_phone`, `chk_token_pin_code`) dengan yang tidak, tanpa penanda mana yang wajib DB-level. | Putuskan per item: implementasikan sebagai `CHECK`/`domain` di DBQuery, atau tandai eksplisit di DBSchema sebagai "Validasi Aplikasi (bukan DB constraint)" agar kolom "Batasan & Aturan Bisnis / FK" tidak lagi ambigu. | ⏳ Dilaporkan (backlog **P2-18**) | DBSchema §3.1, §3.3, §3.4, §3.7, §3.17; DBQuery §1.1, §2.1, §2.4, §3.4 |

**Ringkasan:** Cross-check DDL vs skema acuan menemukan **3 temuan** (1 inkonsistensi tipe data + FK tersembunyi, 1 constraint hilang dari copy-paste yang tidak lengkap, 1 gap ambiguitas dokumentasi lintas 6 kolom) — tidak ada yang bersifat blocker langsung (DDL tetap bisa dieksekusi apa adanya di Aiven PostgreSQL 17), tapi ketiganya berisiko menyebabkan drift lebih lanjut jika salah satu dokumen direvisi tanpa mengecek yang lain. Seluruhnya dilacak sebagai **P1-7, P2-17, P2-18** di §6, belum dikerjakan pada siklus ini sesuai arahan (reporting only).

---

## 11. Tracking Temuan Cross-Check: `README/4-APIDesign.md` vs Dokumentasi Lain (Tahap 4 lanjutan — Google Antigravity)

> **Catatan cakupan & anti-redundansi:** Bagian ini **tidak mengulang isi `README/4-APIDesign.md`** (endpoint, payload, matriks error, traceability-nya sendiri sudah lengkap di dokumen itu) — hanya mencatat **selisih/inkonsistensi** terhadap dokumentasi lain yang relevan dengan penggunaan & transaksi data (FRD, PRD, IA, `3-DBSchema.md`, `3-DBQuery.md`). Setiap baris di §11.2 merujuk balik ke bagian `4-APIDesign.md` yang dimaksud, bukan mengutipnya penuh. Sama seperti siklus 7, **tidak ada perbaikan yang diterapkan langsung** ke `4-APIDesign.md` — seluruh temuan dilaporkan & didaftarkan sebagai backlog baru di §6 (P1-8, P1-9, P2-19 s/d P2-21, + 1 catatan pada P2-10 yang sudah ada).

### 11.1 Metodologi

Setiap endpoint REST/topic MQTT di `4-APIDesign.md` §2-§6 dicocokkan terhadap: kontrak API ringkas FRD §5.5, matriks 9 kode error FRD §8, RBAC FRD §4, aturan bisnis FRD §7, filter/hierarchy layar IA §4, dan tipe/`CHECK`/enum di `3-DBSchema.md` §3-§4 & `3-DBQuery.md` (termasuk temuan P1-7/P2-17/P2-18 dari siklus 7 yang relevan). Fokus pemeriksaan: apakah setiap nilai enum, field, dan aturan validasi yang **baru diperkenalkan** `4-APIDesign.md` (di luar yang sudah eksplisit di FRD) punya jejak balik yang konsisten ke sumber data lain, mengingat dokumen ini akan menjadi acuan langsung implementasi transaksi data ke depan.

### 11.2 Hasil yang Konsisten (Tidak Perlu Tindakan)

| Aspek | Hasil Pengecekan |
| :--- | :--- |
| 9 kode error `4-APIDesign.md` §8 vs matriks error FRD §8 | ✅ Identik 1:1 (kode, HTTP status, semantik fail-safe). |
| Batas `MAX_CONCURRENT_LOCKS_EXCEEDED` "3 slot lock aktif per user" (§8) vs FRD Business Rule 4 (§7) | ✅ Angka sama (3), hanya beda redaksi ("ruangan" vs "slot lock"). |
| `booking_date` "`>= CURRENT_DATE`, maks. 30 hari ke depan" (§7 skema validasi) vs FRD Data Validation Rules | ✅ Identik — FRD juga eksplisit menyebut "Maksimal 30 hari ke depan". |
| `preset_profile` enum (`LECTURE_AI_TRACKING`, `PRESENTATION_CLOSEUP`, `BACKROW_PANORAMA`, §4.1) vs contoh payload FRD §5.1 | ✅ Konsisten — FRD hanya memberi 1 contoh nilai (`LECTURE_AI_TRACKING`) tanpa mendefinisikan enum penuh; `4-APIDesign.md` melengkapinya secara wajar, bukan menyimpang. |
| JWT claims `role`/`tier` enum (§1.2) vs `role_enum`/`subscription_plans.id` DBSchema §4 | ✅ Konsisten (nilai `NONE` pada `tier` adalah status komputasi "tanpa langganan aktif", bukan nilai yang perlu tersimpan di `subscription_plans`). |
| Resolusi P1-6 (payload penuh `/subscriptions/{user_id}/quota` §3.5 & `/bookings/{id}/vault` §4.2, termasuk sentinel `-1` Enterprise) | ✅ Benar-benar melengkapi gap yang sebelumnya ditunda — sentinel `-1` dipakai konsisten dengan `README/3-DBSchema.md` §3.2/§5.2 (hasil perbaikan bug logika siklus 6). |
| Desain "logout = revokasi lokal saja, tanpa blacklist server" (§2.4) | ✅ Tidak kontradiktif — FRD tidak mendefinisikan endpoint logout maupun mekanisme blacklist sama sekali, dan `3-DBSchema.md` §7 (struktur Redis) juga tidak punya key untuk token blacklist, sehingga desain client-side-only tidak menyalahi dokumen manapun. |

### 11.3 Temuan

| # | Temuan | Rekomendasi | Status | Bagian Terdampak |
| :--- | :--- | :--- | :---: | :--- |
| 1 | **Nilai enum MQTT tidak backed DB/FRD (risiko gagal saat implementasi):** payload `/classroom/door/unlock` (§6.2) memakai `reason: "ROOM_MISMATCH"` sebagai opsi ketiga, padahal `door_access_result_enum` (`3-DBSchema.md` §4 / `chk_attempt_result` di `3-DBQuery.md` §3.3) hanya mengizinkan `SUCCESS`/`TOKEN_EXPIRED_OR_INVALID`, dan matriks 9 kode error FRD §8 juga tidak punya `ROOM_MISMATCH`. Alur `3-DBSchema.md` §5.7 secara eksplisit meng-insert nilai `reason`/`result` MQTT ini ke `door_access_attempts.result` yang dibatasi `CHECK` 2 nilai — `INSERT` akan gagal jika `ROOM_MISMATCH` benar dikirim. | Tambahkan `ROOM_MISMATCH` sebagai nilai resmi di DBSchema/DBQuery/matriks error FRD §8 (jika skenario ini nyata dibutuhkan), atau hapus dari payload MQTT `4-APIDesign.md` §6.2. | ⏳ Dilaporkan (backlog **P1-8**) | `4-APIDesign.md` §6.2; `3-DBSchema.md` §3.19/§4; `3-DBQuery.md` §3.3; FRD §8 |
| 2 | **Add-on tanpa definisi FR/FEAT/DB (scope creep):** enum `add_ons` (§7) & contoh payload `POST /bookings/hold` (§3.1) memuat `live_streaming` — tidak ada FR/FEAT, aturan harga, baris breakdown IA H-03, atau kolom database untuk fitur ini (berbeda dari `ai_transcription` yang punya `bookings.is_ai_transcription_addon`). Satu-satunya jejak adalah sebutan sekilas "Live Streaming Multi-platform" sebagai item roadmap bisnis di PD §8. | Masukkan `live_streaming` ke scope MVP secara penuh (FR/FEAT baru, kolom DB, harga, baris IA H-03) sebelum dipakai sebagai enum siap-implementasi, atau keluarkan dari `4-APIDesign.md` §7/§3.1 hingga didefinisikan. | ⏳ Dilaporkan (backlog **P1-9**) | `4-APIDesign.md` §3.1, §7; PD §8; IA §4.3 (H-03) |
| 3 | **`GET /rooms`: filter tidak lengkap & skema respons tidak selaras model data:** query parameter (§2.1: `location`, `facility`, `date`) tidak mencakup 2 dari 4 filter yang dijanjikan IA §4.1 Filter Sidebar H-01 (kapasitas, rentang harga per jam). Skema respons menyisipkan `hourly_rates` berisi ketiga tarif SMALL/MEDIUM/LARGE sekaligus **di dalam satu objek ruangan**, padahal `3-DBSchema.md` §3.5 memodelkan 1 ruangan = 1 `capacity_tier` + 1 `price_per_hour`; field `capacity_tier` itu sendiri tidak muncul di respons (hanya angka kursi `capacity`). | Sinkronkan query params dengan filter IA §4.1 (tambahkan kapasitas & rentang harga); ubah respons agar 1 ruangan mengembalikan `capacity_tier` + `price_per_hour` tunggal miliknya sendiri. | ⏳ Dilaporkan (backlog **P2-19**) | `4-APIDesign.md` §2.1; IA §4.1 (H-01); `3-DBSchema.md` §3.5 |
| 4 | **Drift istilah status Video Vault:** `recording.status`/`transcription.status` pada `GET /bookings/{id}/vault` (§4.2) memakai `COMPLETED`, padahal `3-DBSchema.md` menyimpan `READY` untuk `recordings.status` (§3.10) maupun `ai_transcriptions.status` (§3.11). | Standarkan istilah (pakai `READY` di API, atau dokumentasikan eksplisit sebagai pemetaan/terjemahan `READY`→`COMPLETED` di salah satu dokumen). | ⏳ Dilaporkan (backlog **P2-20**) | `4-APIDesign.md` §4.2; `3-DBSchema.md` §3.10, §3.11 |
| 5 | **Daftar opsi `payment_method` tidak lengkap:** komentar contoh `POST /payments/charge` (§3.2) hanya menyebut 4 dari 7 nilai `payment_method_enum` (`3-DBSchema.md` §4) — `VA_BNI` dan `EWALLET_SHOPEEPAY` tidak disebut (`SUBSCRIPTION_QUOTA` wajar tidak disebut, dipilih sistem bukan pengguna). | Lengkapi komentar/contoh dengan 6 opsi yang bisa dipilih pengguna (semua kecuali `SUBSCRIPTION_QUOTA`). | ⏳ Dilaporkan (backlog **P2-21**) | `4-APIDesign.md` §3.2; `3-DBSchema.md` §4 |
| 6 | **Klaim penyelesaian yang bertentangan dengan status kanonis backlog:** `4-APIDesign.md` §5.6 mengunci `/admin/audit-logs` ke `SUPER_ADMIN`-saja dan §10 mengklaim status **P2-10 "Selesai"** — namun FRD §4 RBAC dikonfirmasi ulang pada siklus ini **masih belum** punya baris permission eksplisit untuk Audit Logs. Klaim ini bertentangan dengan keputusan eksplisit siklus 4 (`docs/X_ProgressSummary.md` §4 Temuan, baris terakhir) bahwa isu ini butuh sign-off produk/keamanan, bukan keputusan sepihak dari dokumen turunan. | Revisi §10 `4-APIDesign.md`: ubah label dari "Selesai" menjadi "Asumsi kerja, menunggu FRD §4" — konsisten dengan cara IA §4.11 (H-11) menandainya. Status **P2-10 di §6 dokumen ini tetap kanonis** dan tidak boleh dianggap tertutup oleh klaim `4-APIDesign.md`. | ⏳ Dilaporkan (catatan pada **P2-10** yang sudah ada) | `4-APIDesign.md` §5.6, §10; FRD §4 |

**Ringkasan:** Cross-check `4-APIDesign.md` menemukan **6 temuan** — 1 di antaranya (`ROOM_MISMATCH`) berisiko menyebabkan kegagalan nyata saat implementasi (pelanggaran `CHECK` constraint), 1 lainnya (`live_streaming`) adalah scope creep tanpa definisi lintas dokumen, dan 1 lagi (`P2-10`) adalah konflik status antar-dokumen yang perlu diluruskan agar backlog `docs/X_ProgressSummary.md` tetap jadi satu-satunya sumber kebenaran status lintas dokumen. Sisanya adalah gap kelengkapan/istilah berisiko rendah. Seluruhnya dilacak sebagai **P1-8, P1-9, P2-19 s/d P2-21** (+ catatan P2-10) di §6, belum dikerjakan pada siklus ini sesuai arahan (reporting only, tanpa menduplikasi isi `4-APIDesign.md` sendiri).

---

## 12. Backlog Tahap 5 — Implementasi API Backend (Menghubungkan UI Prototipe ke Database)

> **Prasyarat tercatat:** Database sudah live di Aiven PostgreSQL (`README/3-DBQuery.md` sudah dieksekusi). Desain kontrak API sudah ada di `README/4-APIDesign.md` v1.0. Backlog di bawah ini adalah **task implementasi kode** (bukan lagi analisis dokumen) — dilacak terpisah dari §6 agar punch list dokumen tidak bercampur dengan pekerjaan engineering harian. Task yang beririsan dengan backlog dokumen lama (P1-7/P1-8/P1-9/P2-17 s/d P2-21) merujuk balik ke ID-nya, bukan mengulang isinya.

| ID | Task | Status | Dokumen Acuan | Catatan |
| :--- | :--- | :---: | :--- | :--- |
| API-01 | Inisialisasi struktur project backend (`server/`, entry point Express) — `express`/`dotenv` sudah ada di `package.json`, tinggal diaktifkan. | ✅ Selesai | `server/index.js` | Struktur `config/`, `middleware/`, `routes/`, `utils/` sudah dipisah dari kode frontend statis. |
| API-02 | Tambahkan `DATABASE_URL` (connection string Aiven) ke `.env` + client `pg`, verifikasi koneksi ke DB yang sudah dibuat. | ✅ Selesai (kode) | `.env.example`, `server/config/db.js` | Pool `pg` sudah ditulis & boot-tested (server start tanpa crash). **Belum diverifikasi live ke Aiven** — perlu isi `.env` asli dengan credential sungguhan dan jalankan smoke test (lihat API-25). |
| API-03 | Middleware dasar: CORS, JSON body parser, error handler global, request logger. | ✅ Selesai | `server/index.js`, `server/middleware/errorHandler.js` | — |
| API-04 | Util JWT (`sign`/`verify`) sesuai klaim `role`/`tier`. | ✅ Selesai | `server/utils/jwt.js` | — |
| API-05 | `POST /auth/register`, `POST /auth/login`. | ✅ Selesai | `server/routes/auth.routes.js` | — |
| API-06 | Middleware `requireAuth` + `requireRole` (5 role RBAC). | ✅ Selesai | `server/middleware/auth.js` | Dipakai di seluruh endpoint terproteksi. |
| API-07 | `GET/PUT /users/me` — backend. | ✅ Backend selesai | `server/routes/users.routes.js` | **Hookup ke `profile.html` (ganti data dummy) belum dikerjakan** — lihat API-23. |
| API-08 | `GET /rooms`. | ✅ Selesai | `server/routes/rooms.routes.js` | P2-19 ikut diselesaikan: filter kapasitas/harga ditambahkan, skema respons dikoreksi jadi 1 ruangan = 1 `capacity_tier` + 1 `price_per_hour`. |
| API-09 | `POST /bookings/hold`. | ✅ Backend selesai (hold saja) | `server/routes/bookings.routes.js` | `POST /bookings/confirm`/payment **tidak diimplementasikan** — H-03 Checkout belum diprototipekan, di luar cakupan siklus ini (hanya H-01/02/06/07/08). Add-on `live_streaming` (P1-9) divalidasi tapi harganya masih ⚠️ Asumsi. |
| API-10 | `POST /bookings/{id}/cancel`. | ⏳ Belum dimulai | FRD §7.2; DBSchema §5.5 | Di luar cakupan siklus ini (bukan bagian H-01/02/06/07/08). |
| API-11 | Hubungkan `index.html` (H-01) ke `GET /rooms` nyata, ganti data statis. | ⏳ Belum dimulai | IA §4.1 | Backend (API-08) sudah siap dipanggil — tinggal kerjakan fetch di sisi frontend. |
| API-12 | `POST /payments/charge` + webhook callback. | ⏳ Belum dimulai | FR-03 | Di luar cakupan siklus ini. |
| API-13 | Hubungkan alur checkout ke endpoint payment nyata. | ⏳ Belum dimulai | H-03 (belum diprototipekan) | Di luar cakupan siklus ini. |
| API-14 | Endpoint token akses pintu + MQTT `/classroom/door/unlock`. | ⏳ Belum dimulai (MQTT) / ✅ Selesai (REST admin override) | FR-04 | `POST /admin/studio/door/unlock` (§5.5, REST) sudah diimplementasikan di `server/routes/admin/door.routes.js`. Topic MQTT `/classroom/door/unlock` (§6.2, ke hardware asli) **belum ditautkan** — broker belum jadi bagian screen yang ada. P1-8 (`ROOM_MISMATCH`) masih belum diputuskan, tapi tidak memblokir jalur REST admin override ini. |
| API-15 | Hubungkan `in-room.html` (H-02) ke endpoint kontrol rekaman/status IoT nyata — backend. | ✅ Backend selesai | `server/routes/studio.routes.js` | `POST /studio/record/action` + `GET /studio/session/{room_id}` (baru, bootstrap timer & status IoT dari Redis) sudah jalan. **Hookup fetch di `in-room.html` belum dikerjakan** — lihat API-23. |
| API-16 | `GET /bookings/{id}/vault`. | ⏳ Belum dimulai | FR-06/FR-07 | Di luar cakupan siklus ini (H-04 belum diprototipekan). |
| API-17 | `GET /subscriptions/{user_id}/quota`. | ✅ Backend selesai | `server/routes/subscriptions.routes.js` | Sentinel `-1` Enterprise diimplementasikan sesuai §5.2 DBSchema. **Hookup badge Keanggotaan di `profile.html` belum dikerjakan.** |
| API-18 | Hubungkan halaman Subscription (H-05). | ⏳ Belum dimulai | H-05 (belum diprototipekan) | Di luar cakupan siklus ini. |
| API-19 | CRUD ruangan, hubungkan `admin/rooms.html` (H-08) — backend. | ✅ Backend selesai (lebih lengkap dari rencana awal) | `server/routes/admin/rooms.routes.js` | Awalnya direncanakan GET/PUT saja — ternyata `admin/rooms.html` butuh CRUD penuh (create/edit/delete/maintenance/diagnostics), semua sudah diimplementasikan sesuai usulan `README/4-APIBackend.md` §4.3. **Hookup fetch di `admin/rooms.html` belum dikerjakan.** |
| API-20 | Endpoint dashboard/ops, hubungkan `admin/dashboard.html` (H-07) — backend. | ✅ Backend selesai | `server/routes/admin/dashboard.routes.js`, `admin/bookings.routes.js`, `admin/door.routes.js` | `GET /admin/dashboard/metrics` + `GET /admin/alerts` (baru, mengisi gap KPI/Live Alert Feed dari `README/4-APIBackend.md` §4.2) + late-checkout (§5.3) + manual unlock (§5.5) semua diimplementasikan. `late_checkout_pending_count` memakai heuristik proxy (⚠️ Asumsi, lihat kode) karena deteksi keterlambatan sengaja manual (P0-3). **Hookup fetch di `admin/dashboard.html` belum dikerjakan.** |
| API-21 | `GET /admin/audit-logs`. | ✅ Backend selesai | `server/routes/admin/audit.routes.js` | Dibatasi `SUPER_ADMIN` sesuai `4-APIDesign.md` §5.6 — P2-10 tetap ditandai terbuka di kode (komentar), bukan diklaim final. |
| API-22 | Endpoint User & Role Management (H-13). | ⏳ Belum dimulai | H-13 (belum diprototipekan) | Di luar cakupan siklus ini. |
| API-23 | Ganti seluruh data dummy di 5 prototipe yang sudah ada dengan pemanggilan API nyata. | ⏳ Belum dimulai | §8 (tracking prototipe) | **Ini yang tersisa** — seluruh endpoint backend untuk H-01/02/06/07/08 sudah siap dipanggil (lihat API-07/08/09/15/17/19/20/21), tinggal kerjakan fetch di sisi HTML satu screen sekaligus. |
| API-24 | Terapkan 9 pesan error user-facing FRD §8 di tiap endpoint yang baru dihubungkan ke frontend. | ⏳ Belum dimulai | FRD §8 | Menunggu API-23 (baru relevan setelah fetch nyata terpasang di frontend). |
| API-25 | Smoke test manual per endpoint terhadap DB Aiven yang sudah live (bukan mock/in-memory). | ⏳ Belum dimulai | — | Kode backend sudah boot-tested lokal (server start + `/health` OK), tapi **belum pernah query ke Aiven sungguhan** — perlu `.env` asli + Redis instance sebelum smoke test end-to-end bisa jalan. |
| API-26 | ~~Update `docs/GEMINI_SYSTEM_PROMPT.md` §"Fase Proyek Saat Ini" begitu backlog ini mulai berjalan.~~ | ✅ Selesai | `docs/GEMINI_SYSTEM_PROMPT.md` →v1.1 | Dikerjakan langsung di siklus 9 ini, sekaligus menuntaskan P2-13 lama. |
| API-27 | Implementasikan 13 endpoint baru yang diusulkan di `README/4-APIBackend.md` §4 (gap ditemukan saat cross-check UI vs `4-APIDesign.md`): bootstrap sesi in-room, KPI & alert feed dashboard, CRUD ruangan, ubah password, toggle 2FA, hapus akun. | ✅ Selesai | `README/4-APIBackend.md` §4; `server/routes/**` | Seluruh 13 endpoint diimplementasikan **kecuali** `GET/PUT /users/me/notifications` (preferensi notifikasi) dan `POST /auth/logout-all` — keduanya sengaja **tidak diimplementasikan** karena masih ⚠️ butuh keputusan skema/arsitektur (lihat P2-22, P2-23 baru di bawah). |

**Catatan siklus 10 (implementasi backend, 9 Agustus 2026):** Backend Node.js/Express untuk **5 screen yang sudah ada prototipenya (H-01, H-02, H-06, H-07, H-08)** sudah diimplementasikan penuh di `server/` — 24 endpoint (11 dari `4-APIDesign.md` v1.0 + 13 dari usulan `4-APIBackend.md` §4, minus 2 yang sengaja ditunda). Server sudah boot-tested lokal (`node server/index.js` + `GET /api/v1/health` → 200 OK) tapi **belum disambungkan ke Aiven/Redis sungguhan** (butuh `.env` asli) dan **belum di-hook ke kode HTML manapun** — kelima prototipe masih memakai data dummy seperti sebelumnya. Endpoint di luar cakupan 5 screen ini (checkout H-03, video vault H-04, subscription H-05, dst.) sengaja tidak dikerjakan sesuai arahan.

**Ringkasan:** 26 task dikelompokkan dalam 10 tahap kerja berurutan (fondasi → auth → katalog/booking → payment → IoT/in-room → vault → subscription → admin portal → integrasi frontend → testing). Belum ada yang dikerjakan pada siklus ini (baru tahap perencanaan backlog). 3 task (API-08, API-09, API-14) sengaja diberi catatan blocker eksplisit karena bergantung pada keputusan bisnis/teknis terbuka (P1-8, P1-9, P2-19) yang belum diselesaikan di §6 — disarankan diputuskan sebelum, bukan sesudah, endpoint terkait ditulis, agar tidak menimbulkan rework.

---

*Dokumen ini sebaiknya diperbarui ulang setiap kali salah satu dari 4 dokumen sumber naik versi mayor, setiap kali progres Tahap 3 Prototyping atau Tahap 5 Implementasi API bertambah signifikan, atau setiap kali `README/3-DBSchema.md`/`README/3-DBQuery.md`/`README/4-APIDesign.md` mengalami revisi signifikan, agar tetap menjadi ringkasan yang akurat.*
